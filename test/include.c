
#include "include.h"

#define multiply *

#define MAX(a,b) (a >b ? a : b)
int main() {
	int local = 2 multiply GLOBAL;	
	int A = 3, B = 7;
	int maximum = MAX(A,B);
	MAX(Gurka , Banan3);
	MAX(A,B);

#undef MAX
	MAX(5,7);
	return local+maximum;
}

